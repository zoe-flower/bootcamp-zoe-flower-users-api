# Code generated by dev-tool; DO NOT EDIT.
name: rollback
on:
  workflow_dispatch:
    inputs:
      sha_to_rollback:
        description: SHA to rollback
        type: string
        required: true

jobs:
  tool_rollback:
    uses: flypay/actions-service-tool/.github/workflows/rollback.yml@v1.0.8
    with:
      gha_deploy_role: ${{ vars.PRODUCTION_GHA_DEPLOY_ROLE }}
      platform_environment: "production"
      sha_to_rollback: ${{ inputs.sha_to_rollback }}
