# Code generated by dev-tool; DO NOT EDIT.
name: Stale
on:
  schedule:
    - cron: '0 0 * * *'  # Run every day at midnight
jobs:
  run-stale:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      - name: Close stale pull requests
        uses: actions/stale@v8
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          days-before-stale: 180
          days-before-close: 7
          stale-issue-label: 'stale'
          stale-pr-label: 'stale'
          exempt-issue-labels: 'not stale'
          exempt-pr-labels: 'not stale'
          operations-per-run: 999
          remove-stale-when-updated: false  # Do not remove stale label when PR is updated
          stale-issue-message: >
            This issue is labeled as requiring an update from the reporter, and no update
            has been received after 180 days. If no update is provided in the next 7 days,
            this issue will be automatically closed. Please use the tag "not stale" if you
            still require this issue.
          stale-pr-message: >
            This PR is labeled as requiring an update from the reporter, and no update
            has been received after 180 days.  If no update is provided in the next 7 days,
            this issue will be automatically closed. Please use the tag "not stale" if you
            still require this PR.
          close-issue-message: >
            This issue has been marked as stale and closed due to no activity for 180 days.
          close-pr-message: >
            This pull request has been marked as stale and closed due to no activity for
            180 days.